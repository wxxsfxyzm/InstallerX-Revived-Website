---
title: 实验室 - InstallerX Revived
description: 了解 InstallerX Revived 的实验性功能和风险选项
---

# 实验室

实验室只保留当前仍属于实验性质或高风险的功能。这些选项可能随版本调整、移除，或在不同 ROM 上表现不同。建议只开启自己确实需要的功能。

## 模块刷写

开启 **启用模块刷写** 后，InstallerX 可以从 ZIP 文件安装支持的 Magisk、KernelSU 或 APatch 模块。

当 ZIP 同时包含应用安装包和模块信息时，InstallerX 会让你选择作为应用安装还是作为模块安装。模块安装是 InstallerX 少数可能使用模块专用命令执行的场景，不完全等同于普通 PackageInstaller 安装。

::: danger
刷写前请确认模块来源可靠。错误模块可能导致系统组件异常或无法正常开机。
:::

## Root 实现方式与字符画

启用模块刷写时，需要选择与当前设备环境匹配的 Root 实现方式：

* **Magisk**
* **KernelSU**
* **APatch**

安装新模块时，InstallerX 可以检测已安装的 Magisk、KernelSU 和 APatch 模块并比较版本。你也可以开启安装开始时显示 InstallerX 字符画。系统安装器模式下是否始终使用 Root 执行特权操作，已经移动到 **设置 -> 安装器设置 -> 授权器参数微调**。

## HTTP 安全策略

已移至[网络设置](./app-settings.md#http-安全策略)。

## GitHub 更新通道

请在[网络设置](./app-settings.md#github-更新通道)中选择。

## 使用建议

日常使用建议保持默认设置。需要排查或测试时，一次只开启一个实验功能，并先用安全 APK 或可信模块验证；如果 ROM 开始跳回系统安装器、安装会话提前失败，或联网下载被阻止，就关闭相关选项。

## 智能授权

默认关闭。开启后，配置所选授权器不可用时，会按启用的后备授权器顺序尝试。候选项支持勾选和拖动排序，至少保留一项；“无”仅在支持系统会话安装的环境中提供。
